import {createSlice} from '@reduxjs/toolkit';

const initialState = {};

const projectParticipantSlice = createSlice({
    name: 'projectParticipant',
    initialState,
    reducers: {},
});

export const {} = projectParticipantSlice.actions;


export default projectParticipantSlice.reducer;
